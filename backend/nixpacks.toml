[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x", "python3", "postgresql", "vips"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'Using enhanced API server - no build needed'"]

[start]
cmd = "npm run start:enhanced"

[variables]
NODE_ENV = "production"
MINIMAL_START = "true"
